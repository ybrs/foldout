[project]
name = "foldout"
version = "0.1.0"
description = "Tool to get snapshots of PostgreSQL databases."
authors = [{name = "Foldout Developers", email = "dev@example.com"}]
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
dependencies = [
    "blake3>=1.0.5",
    "click>=8.1.7",
    "psycopg[binary]>=3.1.18",
]

[project.scripts]
foldout = "foldout.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/foldout"]

[tool.hatch.build.targets.sdist]
include = ["src/foldout"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
pythonpath = ["src"]
